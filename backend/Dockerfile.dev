FROM node:20-alpine

WORKDIR /app

# 安装必要的系统依赖
RUN apk add --no-cache openssl

# 复制 package 文件
COPY package*.json ./
COPY prisma ./prisma/

# 安装依赖
RUN npm ci

# 生成 Prisma Client
RUN npx prisma generate

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 启动命令 (开发模式)
CMD ["npm", "run", "dev"]
